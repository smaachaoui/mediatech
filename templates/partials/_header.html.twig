<nav class="navbar navbar-expand-lg navbar-dark mt-navbar">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center gap-2" href="{{ path('app_home') }}">
      <span class="fw-semibold">MediaTech</span>
    </a>
    <button class="navbar-toggler mt-navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#mainNavbar"
            aria-controls="mainNavbar"
            aria-expanded="false"
            aria-label="Ouvrir le menu">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="mainNavbar">
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_catalog') }}">Catalogue</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_collections_public') }}">Nos collections</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_about') }}">À propos</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_contact') }}">Contact</a>
        </li>
      </ul>
      <ul class="navbar-nav mb-2 mb-lg-0">
        {% if app.user %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_profile') }}">Mon profil</a>
          </li>
          {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
              <a class="nav-link" href="#">Admin</a>
            </li>
          {% endif %}
          <li class="nav-item">
            <a class="btn btn-outline-light btn-sm ms-lg-2" href="{{ path('app_logout') }}">
              Déconnexion
            </a>
          </li>
        {% else %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-light btn-sm ms-lg-2 mt-auth-btn" href="{{ path('app_login') }}">
              Connexion
            </a>
          </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>