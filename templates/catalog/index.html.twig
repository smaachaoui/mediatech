{% extends 'base.html.twig' %}

{% block title %}Catalogue | MediaTech{% endblock %}

{% block body %}

{# =======================
   MAIN
   ======================= #}
<div class="d-flex flex-column gap-3">

  {# =======================
     search-section
     (titre + barre de recherche)
     ======================= #}
  <section class="bg-white border rounded-3 p-3">
    <h1 class="h5 mb-3 text-center">Vous cherchez un livre ou un film ?</h1>

    <form method="get" action="{{ path('app_catalog') }}" class="row g-2 align-items-center">
      <div class="col-12 col-md">
        <div class="input-group">
          <span class="input-group-text" aria-hidden="true">🔎</span>
          <input
            type="search"
            name="q"
            value="{{ q }}"
            class="form-control"
            placeholder="Titre, auteur, réalisateur..."
          >
        </div>
      </div>

      {# On conserve le filtre type depuis la recherche #}
      <input type="hidden" name="type" value="{{ type }}">

      <div class="col-12 col-md-auto">
        <button class="btn btn-primary w-100" type="submit">Rechercher</button>
      </div>
    </form>
  </section>

  {# =======================
     results-section
     (aside filtres + résultats)
     ======================= #}
  <section class="bg-white border rounded-3 p-3">

    <div class="row g-4">

      {# ASIDE filtres (wireframe) #}
      <aside class="col-12 col-lg-3">
        <div class="border rounded-3 p-3">
          <h2 class="h6 mb-3">Filtrer</h2>

          <form method="get" action="{{ path('app_catalog') }}">
            <div class="mb-3">
              <label class="form-label">Type</label>
              <select class="form-select" name="type">
                <option value="all"    {{ type == 'all' ? 'selected' : '' }}>Tout</option>
                <option value="books"  {{ type == 'books' ? 'selected' : '' }}>Livres</option>
                <option value="movies" {{ type == 'movies' ? 'selected' : '' }}>Films</option>
              </select>
            </div>

            {# TRI: UI-only pour l’instant (pas câblé côté API) #}
            <div class="mb-3">
              <label class="form-label">Tri</label>
              <select class="form-select" name="sort" disabled>
                <option value="recent" selected>Les plus récents</option>
                <option value="title">Titre A → Z</option>
              </select>
              <div class="form-text">Le tri sera activé dans une étape suivante.</div>
            </div>

            {# On conserve la recherche si présente #}
            {% if q %}
              <input type="hidden" name="q" value="{{ q }}">
            {% endif %}

            <div class="d-grid gap-2">
              <button class="btn btn-dark" type="submit">Appliquer</button>
              <a class="btn btn-outline-secondary" href="{{ path('app_catalog') }}">Réinitialiser</a>
            </div>
          </form>
        </div>
      </aside>

      {# ZONE résultats #}
      <div class="col-12 col-lg-9">

        {# Normalisation : on dérive books/movies depuis results #}
        {% set books = results.books ?? [] %}
        {% set movies = results.movies ?? [] %}
        {% set hasAny = (books|length) + (movies|length) > 0 %}

        {# Si aucune recherche saisie, on affiche un état neutre #}
        {% if not q %}
          {% include 'components/_state_empty.html.twig' with {
            title: 'Recherchez un média',
            message: 'Saisissez un titre (livre ou film) puis lancez la recherche.'
          } %}
        {% else %}

          {# Si recherche mais aucun résultat #}
          {% if not hasAny %}
            {% include 'components/_state_empty.html.twig' with {
              title: 'Aucun résultat',
              message: 'Essayez de modifier vos filtres ou votre recherche.'
            } %}
          {% else %}

            {# Si "all", deux blocs. Si "books" ou "movies", un seul bloc. #}

            {% if type in ['all','books'] %}
              <div class="d-flex justify-content-between align-items-center mb-2">
                <h3 class="h6 mb-0">Livres</h3>
              </div>

              {% if books is empty %}
                <div class="text-muted small mb-4">Aucun livre trouvé.</div>
              {% else %}
                <div class="row g-3 mb-4">
                  {% for book in books %}
                    <div class="col-6 col-md-4 col-lg-3">
                      {% include 'components/_structure_media.html.twig' with { item: book, kind: 'book' } only %}
                    </div>
                  {% endfor %}
                </div>
              {% endif %}
            {% endif %}

            {% if type in ['all','movies'] %}
              <div class="d-flex justify-content-between align-items-center mb-2">
                <h3 class="h6 mb-0">Films</h3>
              </div>

              {% if movies is empty %}
                <div class="text-muted small">Aucun film trouvé.</div>
              {% else %}
                <div class="row g-3">
                  {% for movie in movies %}
                    <div class="col-6 col-md-4 col-lg-3">
                      {% include 'components/_structure_media.html.twig' with { item: movie, kind: 'movie' } only %}
                    </div>
                  {% endfor %}
                </div>
              {% endif %}
            {% endif %}

          {% endif %}
        {% endif %}

      </div>

    </div>
  </section>

</div>

{% endblock %}
